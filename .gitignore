node_modules
dist
dist-ssr
*.local

# Supabase keys live here (1f1). Never commit.
.env
.env.*
!.env.example

# Supabase CLI scratch: which project this machine is linked to, and local db state.
supabase/.temp
supabase/.branches

# Render checks from scripts/shoot.mjs. Useful to look at, pointless to version.
screenshots

# Ad-hoc browser drivers and one-off captures, written to answer a question and then finished
# with. They live at the root because playwright resolves from the project; they are nobody's
# reference and they must not be linted.
/.*.mjs
/shot-*.png

.DS_Store
Thumbs.db

.vscode/*
!.vscode/extensions.json
.idea
*.log
npm-debug.log*
